#include <stdio.h>

/*
int main(Void) (
    INT sum;
    /* COMPUTE RESULT
    sum = 25 + 37 - 19
    /* DISPLAY RESULTS //
    printf ("The answer is &i\n", sum);
    return 0;
}
*/

// Corrected version
int main(void)
{
    int sum;

    // Compute result
    sum = 25 + 37 - 19;

    // Display result
    printf("The answer is %i\n", sum);
    return 0;
};